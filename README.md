# BoardingValidatorDunkle

To run and use the program, simply compile the code in VS Code and enter the requested data in the console.

The program will request information on:
- Animal name
- Animal size
- Animal amount
- If the animal is a carnivore

It will then reject or board the animal accordingly.
